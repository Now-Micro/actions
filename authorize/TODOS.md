# TODOS

- need to verify populateUsers.js works
- need to update permissions.json
- need to refactor the action to use the users.json aliases (think of best Data Structure to use)
- need to add the authorize action to the reusable nuget publish workflow (any other actions need it in any other repos?)
- need to add the authorize action to any other repo's workflows (CSI-Connector, CodeBits, WarrantyService)?