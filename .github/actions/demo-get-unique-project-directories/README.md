# Demo Get Unique Project Directories

This demo action exercises the nearest-project-directory resolver with pattern matching and fallback behavior.

## Usage

```yaml
- name: Run project directory demo
  uses: ./.github/actions/demo-get-unique-project-directories
```