# Demo GitHub Release

This demo action exercises the GitHub release action in dry-run mode so release-note and packaging behavior can be validated safely.

## Usage

```yaml
- name: Run GitHub release demo
  uses: ./.github/actions/demo-github-release
```