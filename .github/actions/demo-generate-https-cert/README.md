# Demo Generate HTTPS Certificate

This demo action exercises the HTTPS certificate generator in both relative and absolute working-directory scenarios.

## Usage

```yaml
- name: Run HTTPS cert demo
  uses: ./.github/actions/demo-generate-https-cert
```