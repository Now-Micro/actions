# Demo Checks Workflow

This demo action exercises the reusable checks workflow by running the repository's lint, coding standards, and test steps together.

## Usage

```yaml
- name: Run checks demo
  uses: ./.github/actions/demo-checks-workflow
```