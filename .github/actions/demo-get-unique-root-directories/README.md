# Demo Get Unique Root Directories

This demo action exercises the unique root directory detector against a representative set of changed paths.

## Usage

```yaml
- name: Run root directory demo
  uses: ./.github/actions/demo-get-unique-root-directories
```